#version 330 core

uniform int iFrame;
uniform sampler2D texSampler;

in vec3 color;
in vec2 texCoord;

out vec4 FragColor;

void main()
{
	FragColor = texture(texSampler, texCoord) * vec4(color, 1.0);
}